<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title><%= content_for(:title) || "My Portfolio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    
    <!-- Trix Rich Text Editor (FOSS Alternative) -->
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.8/dist/trix.css">
    <script type="text/javascript" src="https://unpkg.com/trix@2.0.8/dist/trix.umd.min.js"></script>
    
    <!-- Custom Trix Editor Styling -->
    <style>
      /* Ensure Trix editor has white background and dark text for all themes */
      trix-editor {
        background-color: #ffffff !important;
        color: #000000 !important;
        border: 1px solid #d1d5db !important;
        border-radius: 0.375rem !important;
        min-height: 120px !important;
      }
      
      trix-editor:focus {
        outline: none !important;
        border-color: #3b82f6 !important;
        box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2) !important;
      }
      
      /* Style the toolbar to match */
      trix-toolbar {
        background-color: #f9fafb !important;
        border: 1px solid #d1d5db !important;
        border-bottom: none !important;
        border-radius: 0.375rem 0.375rem 0 0 !important;
      }
      
      trix-toolbar .trix-button-group {
        background-color: transparent !important;
      }
      
      trix-toolbar .trix-button {
        background-color: transparent !important;
        color: #374151 !important;
        border: none !important;
      }
      
      trix-toolbar .trix-button:hover {
        background-color: #e5e7eb !important;
      }
      
      trix-toolbar .trix-button.trix-active {
        background-color: #dbeafe !important;
        color: #1d4ed8 !important;
      }
    </style>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.svg?v=2" type="image/svg+xml">
    <link rel="icon" href="/icon.png?v=2" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png?v=2">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    
    <style>
      :root {
        --bg-primary: #ffffff;
        --bg-secondary: #f8f9fa;
        --text-primary: #333;
        --text-secondary: #666;
        --border-color: #ddd;
        --link-color: #007bff;
      }
      
      [data-theme="dark"] {
        --bg-primary: #1a1a1a;
        --bg-secondary: #2d2d2d;
        --text-primary: #ffffff;
        --text-secondary: #cccccc;
        --border-color: #444;
        --link-color: #4da3ff;
      }
      
      [data-theme="f1"] {
        --bg-primary: #15151E;
        --bg-secondary: #1E1E28;
        --text-primary: #ffffff;
        --text-secondary: #E8E8E8;
        --border-color: #E10600;
        --link-color: #E10600;
      }
      
      body {
        background-color: var(--bg-primary);
        color: var(--text-primary);
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        transition: background-color 0.3s, color 0.3s;
      }
      
      a {
        color: var(--link-color);
      }
      
      .theme-switcher {
        display: flex;
        gap: 0.5rem;
        align-items: center;
      }
      
      .theme-btn {
        padding: 0.5rem 1rem;
        border: 2px solid var(--border-color);
        background: var(--bg-secondary);
        color: var(--text-primary);
        border-radius: 6px;
        cursor: pointer;
        font-size: 0.9rem;
        transition: all 0.3s;
      }
      
      .theme-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      }
      
      .theme-btn.active {
        background: var(--link-color);
        color: white;
        border-color: var(--link-color);
      }
    </style>
  </head>

  <body>
    <header style="background: var(--bg-secondary); border-bottom: 2px solid var(--border-color); padding: 1rem 2rem;">
      <nav style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
        <div style="display: flex; gap: 2rem; align-items: center;">
          <%= link_to 'Alex Pluskett', root_path, style: "font-size: 1.5rem; font-weight: bold; text-decoration: none; color: var(--text-primary);" %>
          <ul style="list-style: none; display: flex; gap: 1.5rem; padding: 0; margin: 0;">
            <li><%= link_to 'Home', root_path, style: "text-decoration: none; font-weight: 500;" %></li>
            <li><%= link_to 'Demos', demos_path, style: "text-decoration: none; font-weight: 500;" %></li>
          </ul>
        </div>
        
        <div class="theme-switcher">
          <span style="font-size: 0.9rem; color: var(--text-secondary);">Theme:</span>
          <button class="theme-btn" data-theme-btn="light">☀️ Light</button>
          <button class="theme-btn" data-theme-btn="dark">🌙 Dark</button>
          <button class="theme-btn" data-theme-btn="f1">🏎️ F1</button>
        </div>
      </nav>
    </header>
    
    <!-- Course Management Navigation (only show on course pages) -->
    <% if request.path.start_with?('/courses', '/sections', '/students', '/prefixes') %>
      <nav style="background: var(--bg-secondary); border-bottom: 2px solid var(--border-color); padding: 0.75rem 2rem; margin-bottom: 2rem;">
        <div style="max-width: 1200px; margin: 0 auto;">
          <ul style="list-style: none; display: flex; gap: 2rem; padding: 0; margin: 0;">
            <li><%= link_to '📚 Courses', courses_path, style: "text-decoration: none; color: var(--link-color); font-weight: #{request.path.start_with?('/courses') ? 'bold' : 'normal'};" %></li>
            <li><%= link_to '📝 Sections', sections_path, style: "text-decoration: none; color: var(--link-color); font-weight: #{request.path.start_with?('/sections') ? 'bold' : 'normal'};" %></li>
            <li><%= link_to '👨‍🎓 Students', students_path, style: "text-decoration: none; color: var(--link-color); font-weight: #{request.path.start_with?('/students') ? 'bold' : 'normal'};" %></li>
            <li><%= link_to '🏷️ Prefixes', prefixes_path, style: "text-decoration: none; color: var(--link-color); font-weight: #{request.path.start_with?('/prefixes') ? 'bold' : 'normal'};" %></li>
          </ul>
        </div>
      </nav>
    <% end %>
    </header>

    <main style="min-height: 70vh;">
      <%= yield %>
    </main>
    
    <footer style="background: var(--bg-secondary); border-top: 2px solid var(--border-color); padding: 2rem; margin-top: 4rem; text-align: center;">
      <p style="color: var(--text-secondary); margin: 0;">
        Built with Ruby on Rails • <%= link_to 'View on GitHub', 'https://github.com', target: '_blank' %>
      </p>
    </footer>
    
    <script>
      // Theme switcher functionality
      const html = document.documentElement;
      const themeButtons = document.querySelectorAll('[data-theme-btn]');
      
      // Load saved theme or default to light
      const savedTheme = localStorage.getItem('theme') || 'light';
      html.setAttribute('data-theme', savedTheme);
      updateActiveButton(savedTheme);
      
      themeButtons.forEach(button => {
        button.addEventListener('click', () => {
          const theme = button.getAttribute('data-theme-btn');
          html.setAttribute('data-theme', theme);
          localStorage.setItem('theme', theme);
          updateActiveButton(theme);
        });
      });
      
      function updateActiveButton(theme) {
        themeButtons.forEach(btn => {
          if (btn.getAttribute('data-theme-btn') === theme) {
            btn.classList.add('active');
          } else {
            btn.classList.remove('active');
          }
        });
      }
    </script>
  </body>
</html>
